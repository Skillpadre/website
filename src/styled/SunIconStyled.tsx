import styled from '@emotion/styled';
import LightModeIcon from '@mui/icons-material/LightMode';

export default function SunIconStyled(){

    const SunIcon = styled(LightModeIcon)`

    `
    return (
        <SunIcon>
        </SunIcon>   
    )
}